# 当前阶段开发计划

## 阶段目标

**当前阶段：完善检测功能，达到能够识别 hand, noodle_rope, noodle_bundle 的程度**

## 当前任务

### 1. 训练检测模型（进行中）
- ✅ 标注了7个视频（cm1-cm7）
- ⏳ 正在训练检测模型
- ⏳ 测试检测效果

### 2. 实现姿态估计（可选，独立功能）
- ✅ 已创建姿态估计模块框架
- ⏳ 可以使用预训练模型（MediaPipe/MMPose）
- ⏳ 不影响检测功能，独立运行

### 3. 代码结构优化
- ✅ 为后续模块预留接口
- ✅ 保持模块独立性
- ✅ 方便后续扩展

## 模块状态

| 模块 | 状态 | 说明 |
|------|------|------|
| **检测模块** | ✅ 主要功能 | 训练中，识别hand, noodle_rope, noodle_bundle |
| **姿态估计** | ✅ 可选功能 | 已实现框架，可使用预训练模型，独立运行 |
| **粗细分析** | 📋 预留接口 | 代码框架已创建，后续添加 |
| **评分模块** | 📋 预留接口 | 代码框架已创建，后续添加 |
| **报告生成** | 📋 预留接口 | 后续添加 |

## 当前不需要的功能

- ❌ 动作规范性评估
- ❌ 标准动作对比
- ❌ 综合评分报告
- ❌ 面条粗细均匀度评估（仅保留接口）

## 后续扩展预留

代码中已为以下功能预留接口：
1. **姿态分析** (`PoseAnalyzer` 类)
2. **动作规范性评分** (`ActionNormalityScorer` 类)
3. **粗细分类器** (`ThicknessClassifier` 类)
4. **粗细回归模型** (`ThicknessRegressionModel` 类)
5. **评分器扩展** (`Scorer` 类的各种方法)

需要时可以直接扩展这些接口，不会影响现有功能。

## 下一步

1. 等待检测模型训练完成
2. 测试检测效果
3. 如果效果好，继续使用；如果不好，继续标注更多数据
4. 需要时再添加姿态估计和粗细分析功能

